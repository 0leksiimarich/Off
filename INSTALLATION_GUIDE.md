# 📚 Детальна інструкція зі встановлення AI Друг

## Передумови

### 1. Встановіть Node.js
Завантажте та встановіть Node.js версії 18 або вище:
- Сайт: https://nodejs.org/
- Рекомендується LTS версія

Перевірте встановлення:
```bash
node --version  # Має бути >= 18.0.0
npm --version
```

### 2. Отримайте Gemini API ключ

1. Перейдіть на https://makersuite.google.com/app/apikey
2. Увійдіть з вашим Google аккаунтом
3. Натисніть кнопку "Create API Key"
4. Скопіюйте згенерований ключ (починається з "AI...")
5. Збережіть його в безпечному місці

## Крок за кроком встановлення

### Крок 1: Розпакуйте проект
Розпакуйте архів `ai-friend.zip` у бажану папку.

### Крок 2: Відкрийте термінал
- **Windows**: Натисніть Win+R, введіть `cmd`, Enter
- **Mac/Linux**: Відкрийте Terminal

Перейдіть до папки проекту:
```bash
cd шлях/до/ai-friend
```

### Крок 3: Встановіть залежності
```bash
npm install
```

Це може зайняти 2-5 хвилин залежно від швидкості інтернету.

### Крок 4: Налаштуйте змінні середовища

Створіть файл `.env` у кореневій папці проекту:

**Windows (Command Prompt):**
```bash
copy .env.example .env
```

**Mac/Linux:**
```bash
cp .env.example .env
```

Відкрийте файл `.env` у текстовому редакторі та вставте ваш Gemini API ключ:
```env
VITE_GEMINI_API_KEY=ваш_фактичний_ключ_тут
```

### Крок 5: Запустіть проект
```bash
npm run dev
```

Ви побачите повідомлення:
```
VITE v5.x.x ready in xxx ms

➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
```

### Крок 6: Відкрийте додаток
Відкрийте браузер та перейдіть на http://localhost:3000

## Можливі проблеми та рішення

### Проблема 1: "npm: command not found"
**Рішення:** Node.js не встановлено або не додано до PATH
1. Встановіть Node.js з https://nodejs.org/
2. Перезапустіть термінал

### Проблема 2: "VITE_GEMINI_API_KEY is not defined"
**Рішення:** API ключ не налаштовано
1. Переконайтеся, що файл `.env` існує
2. Перевірте, що ключ вставлено без лапок
3. Перезапустіть dev server

### Проблема 3: "Failed to fetch" при спробі надіслати повідомлення
**Рішення:** Невірний API ключ
1. Перевірте ключ на https://makersuite.google.com/app/apikey
2. Переконайтеся, що ключ активний
3. Оновіть `.env` файл
4. Перезапустіть сервер (Ctrl+C, потім `npm run dev`)

### Проблема 4: Порт 3000 вже використовується
**Рішення:** Змініть порт
Відкрийте `vite.config.ts` та змініть порт:
```typescript
server: {
  port: 3001, // або інший вільний порт
}
```

## Build для production

Коли проект готовий до деплою:

### 1. Створіть production build
```bash
npm run build
```

Це створить папку `dist/` з оптимізованими файлами.

### 2. Перегляньте production версію локально
```bash
npm run preview
```

### 3. Деплой
Завантажте вміст папки `dist/` на ваш хостинг (Netlify, Vercel, GitHub Pages тощо).

## Налаштування Firebase (опціонально)

Якщо хочете додати аутентифікацію:

### 1. Створіть Firebase проект
1. Перейдіть на https://console.firebase.google.com
2. Натисніть "Add project"
3. Слідуйте інструкціям

### 2. Додайте Web App
1. В проекті натисніть "Add app" → Web
2. Дайте назву додатку
3. Скопіюйте конфігурацію

### 3. Додайте до .env
```env
VITE_FIREBASE_API_KEY=...
VITE_FIREBASE_AUTH_DOMAIN=...
VITE_FIREBASE_PROJECT_ID=...
VITE_FIREBASE_STORAGE_BUCKET=...
VITE_FIREBASE_MESSAGING_SENDER_ID=...
VITE_FIREBASE_APP_ID=...
```

### 4. Розкоментуйте код Firebase
Відкрийте `src/services/firebaseService.ts` та розкоментуйте весь код.

## Оновлення проекту

Для оновлення залежностей:
```bash
npm update
```

Для оновлення всіх до найновіших версій:
```bash
npm install -g npm-check-updates
ncu -u
npm install
```

## Корисні команди

| Команда | Опис |
|---------|------|
| `npm run dev` | Запустити dev server |
| `npm run build` | Створити production build |
| `npm run preview` | Перегляд production версії |
| `npm run lint` | Перевірити код на помилки |

## Підтримка

Якщо виникли проблеми:
1. Перевірте, чи всі кроки виконані правильно
2. Перегляньте логи в терміналі
3. Перевірте консоль браузера (F12 → Console)
4. Перезапустіть dev server

Успішної роботи з AI Друг! 🚀
